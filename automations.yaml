- alias: Log Level
  trigger:
    platform: state
    entity_id: input_select.log_level
  action:
    service: logger.set_level
    data_template:
      homeassistant.components: "{{ trigger.to_state.state }}"

- alias: GoogleHome Volume 
  trigger:
    - platform: state
      entity_id: input_number.google_home_living_room_volume
  action:
    - service: media_player.volume_set
      data_template:
        entity_id: media_player.living_room
        volume_level: '{{ trigger.to_state.state }}'
#trigger:
#  platform: template 
#  value_template: "{% if is_state('light.garden', 'on') %}true{% endif %}" 
#condition:
#  condition: state
#  entity_id: light.garden
#  state: 'off'
#action:
#  - service: homeassistant.turn_off
#    entity_id: switch.handheld_remote_button_d
#  - service: light.turn_on
#    entity_id: light.garden
#        data:
#          rgb_color: [255,188,75]

#trigger:
#  platform: state
#  value_template: "{% if is_state('light.garden', 'off') %}true{% endif %}" 
#condition:
#  condition: state
#  entity_id: light.garden
#  state: 'on'
#action:
#  - service: homeassistant.turn_off
#    entity_id: switch.handheld_remote_button_d
#  - service: light.turn_off
#    entity_id: light.garden
