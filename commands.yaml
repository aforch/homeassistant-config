shell_command:
  rebootpi: 'sudo reboot'
  shutdownpi: 'sudo shutdown -h now'
#script:
#  reboot_pi:
#    alias: Reboot the Pi
#    sequence:
#      - service: shell_command.rebootpi

#shutdown_shell_command:
#  shutdownpi: 'sudo shutdown -h now'
    
#script:
#  shutdown_pi:
#    alias: Shutdown the Pi
#    sequence:
#      - service: shell_command.shutdown
